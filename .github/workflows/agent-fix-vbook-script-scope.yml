name: Agent fix vBook script scope

on:
  push:
    branches:
      - agent/generalize-browser-session-fetch
    paths:
      - scripts/agent_fix_vbook_script_scope.py
      - .github/workflows/agent-fix-vbook-script-scope.yml

permissions:
  contents: write

jobs:
  patch-and-verify:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: agent/generalize-browser-session-fetch
          fetch-depth: 0

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Apply generic vBook script realm fix
        id: patch
        continue-on-error: true
        run: python3 scripts/agent_fix_vbook_script_scope.py

      - name: Verify patch formatting
        id: diffcheck
        if: steps.patch.outcome == 'success'
        continue-on-error: true
        run: git diff --check

      - name: Run source-vBook tests with captured output
        id: tests
        if: steps.patch.outcome == 'success' && steps.diffcheck.outcome == 'success'
        continue-on-error: true
        shell: bash
        run: |
          set -o pipefail
          ./gradlew --no-daemon :source-vbook:test --stacktrace 2>&1 | tee /tmp/vbook-script-scope-test.log

      - name: Publish verified fix or detailed failure beacon
        if: always()
        env:
          PATCH_OUTCOME: ${{ steps.patch.outcome }}
          DIFF_OUTCOME: ${{ steps.diffcheck.outcome }}
          TEST_OUTCOME: ${{ steps.tests.outcome }}
        run: |
          mkdir -p .agent
          printf 'patch=%s\ndiffcheck=%s\ntests=%s\n' "$PATCH_OUTCOME" "$DIFF_OUTCOME" "$TEST_OUTCOME" > .agent/vbook-script-scope-status.txt
          if [ -f /tmp/vbook-script-scope-test.log ] && [ "$TEST_OUTCOME" != success ]; then
            tail -n 500 /tmp/vbook-script-scope-test.log > .agent/vbook-script-scope-test.log
          fi
          if [ "$PATCH_OUTCOME" = success ] && [ "$DIFF_OUTCOME" = success ] && [ "$TEST_OUTCOME" = success ]; then
            rm -f scripts/agent_fix_vbook_script_scope.py .github/workflows/agent-fix-vbook-script-scope.yml .agent/vbook-script-scope-status.txt .agent/vbook-script-scope-test.log
            message='fix: run vBook loads in shared Chromium script realm'
          else
            message='ci: record detailed vBook script realm test failure'
          fi
          git add -A
          if git diff --cached --quiet; then
            exit 0
          fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git commit -m "$message"
          git push origin HEAD:agent/generalize-browser-session-fetch

      - name: Fail workflow when verification failed
        if: always() && (steps.patch.outcome != 'success' || steps.diffcheck.outcome != 'success' || steps.tests.outcome != 'success')
        run: exit 1
