name: Build Freesound Mode 3 diagnostics V6 retry

on:
  push:
    branches:
      - agent/freesound-stage-1
    paths:
      - ".github/workflows/build-freesound-diagnostics-v6-retry.yml"

permissions:
  contents: write

concurrency:
  group: build-freesound-diagnostics-v6-retry
  cancel-in-progress: false

jobs:
  verify:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: agent/freesound-stage-1
          fetch-depth: 0

      - name: Record run start
        shell: bash
        run: |
          set -e
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          {
            echo "RUN_ID=${GITHUB_RUN_ID}"
            echo "RUN_ATTEMPT=${GITHUB_RUN_ATTEMPT}"
            echo "TRIGGER_SHA=${GITHUB_SHA}"
            echo "STARTED_AT=$(date -u +%Y-%m-%dT%H:%M:%SZ)"
          } > MODE3_DIAGNOSTICS_RUN_V6_RETRY.txt
          git add -- MODE3_DIAGNOSTICS_RUN_V6_RETRY.txt
          git commit -m "Record Mode 3 diagnostics retry start"
          git push origin HEAD:agent/freesound-stage-1

      - name: Set up JDK 17
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "17"

      - name: Set up Gradle cache
        uses: gradle/actions/setup-gradle@v4

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Install Android SDK 36
        shell: bash
        run: |
          yes | sdkmanager --licenses >/dev/null || true
          sdkmanager "platform-tools" "platforms;android-36" "build-tools;36.0.0"

      - name: Patch diagnostics, test and build
        id: verify
        shell: bash
        run: |
          set +e
          rm -f /tmp/patch_v6.log /tmp/test_v6.log /tmp/build_v6.log /tmp/result_v6_retry.txt
          python3 .github/scripts/mode3_diag_patch_v6.py > /tmp/patch_v6.log 2>&1
          PATCH_STATUS=$?
          TEST_STATUS=99
          BUILD_STATUS=99
          if [ "$PATCH_STATUS" -eq 0 ]; then
            ./gradlew --no-daemon --stacktrace :app:testDebugUnitTest > /tmp/test_v6.log 2>&1
            TEST_STATUS=$?
          fi
          if [ "$TEST_STATUS" -eq 0 ]; then
            ./gradlew --no-daemon --stacktrace :app:assembleDebug > /tmp/build_v6.log 2>&1
            BUILD_STATUS=$?
          fi
          SUCCESS=false
          if [ "$PATCH_STATUS" -eq 0 ] && [ "$TEST_STATUS" -eq 0 ] && [ "$BUILD_STATUS" -eq 0 ] && [ -f app/build/outputs/apk/debug/app-debug.apk ]; then
            SUCCESS=true
          fi
          {
            echo "RUN_ID=${GITHUB_RUN_ID}"
            echo "RUN_ATTEMPT=${GITHUB_RUN_ATTEMPT}"
            echo "TRIGGER_SHA=${GITHUB_SHA}"
            echo "SUCCESS=${SUCCESS}"
            echo "PATCH_STATUS=${PATCH_STATUS}"
            echo "TEST_STATUS=${TEST_STATUS}"
            echo "BUILD_STATUS=${BUILD_STATUS}"
            echo "ARTIFACT_NAME=NgheTruyen-freesound-diagnostics-v6-retry-debug"
            echo
            echo "=== PATCH LOG ==="
            tail -n 260 /tmp/patch_v6.log 2>/dev/null || true
            echo
            echo "=== TEST LOG ==="
            tail -n 360 /tmp/test_v6.log 2>/dev/null || true
            echo
            echo "=== BUILD LOG ==="
            tail -n 420 /tmp/build_v6.log 2>/dev/null || true
          } > /tmp/result_v6_retry.txt
          echo "success=${SUCCESS}" >> "$GITHUB_OUTPUT"
          exit 0

      - name: Upload APK
        if: steps.verify.outputs.success == 'true'
        uses: actions/upload-artifact@v4
        with:
          name: NgheTruyen-freesound-diagnostics-v6-retry-debug
          retention-days: 30
          if-no-files-found: error
          path: app/build/outputs/apk/debug/app-debug.apk

      - name: Commit verified diagnostics or error report
        if: always()
        shell: bash
        run: |
          set -e
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          if [ "${{ steps.verify.outputs.success }}" = "true" ]; then
            for file in \
              app/src/main/java/vn/nghetruyen/app/freesound/FreesoundAutoAudioResolver.kt \
              app/src/main/java/vn/nghetruyen/app/ai/NarrationPlanCoordinator.kt \
              app/src/main/java/vn/nghetruyen/app/playback/ReaderPlaybackService.kt \
              app/src/main/java/vn/nghetruyen/app/playback/AudioDirectionRuntime.kt
            do
              git add -- "$file"
            done
          else
            git reset --hard HEAD
          fi
          cp /tmp/result_v6_retry.txt MODE3_DIAGNOSTICS_BUILD_RESULT_V6_RETRY.txt
          git add -- MODE3_DIAGNOSTICS_BUILD_RESULT_V6_RETRY.txt
          if ! git diff --cached --quiet; then
            if [ "${{ steps.verify.outputs.success }}" = "true" ]; then
              git commit -m "Add deep Mode 3 Freesound diagnostics"
            else
              git commit -m "Record Mode 3 diagnostics retry failure"
            fi
            git push origin HEAD:agent/freesound-stage-1
          fi

      - name: Mark failed verification
        if: steps.verify.outputs.success != 'true'
        run: exit 1
