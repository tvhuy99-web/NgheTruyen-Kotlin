name: Mode 3 dedupe APK only

on:
  workflow_dispatch:

jobs:
  archived-build:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Temporary APK build completed in run 32570328927."
